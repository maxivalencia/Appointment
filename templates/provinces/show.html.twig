{% extends 'base.html.twig' %}

{% block title %}Provinces{% endblock %}

{% block body %}
    <h1>Provinces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ province.id }}</td>
            </tr>
            <tr>
                <th>Province</th>
                <td>{{ province.province }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_provinces_index') }}" class="btn-action back"><i class="fa fa-arrow-left"></i> Retour</a>

    <a href="{{ path('app_provinces_edit', {'id': province.id}) }}" class="btn-action edit">edition</a>

    {{ include('provinces/_delete_form.html.twig') }}
{% endblock %}
