<form method="post" action="{{ path('app_nombre_vehicule_maximum_rendez_vous_delete', {'id': nombre_vehicule_maximum_rendez_vou.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer cette élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nombre_vehicule_maximum_rendez_vou.id) }}">
    <button class="btn-action delete"><i class="fas fa-trash"></i> Supprimer</button>
</form>
