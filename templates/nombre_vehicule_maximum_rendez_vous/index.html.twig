{% extends 'base.html.twig' %}

{% block title %}Liste du Nombre Véhicule Maximum par Rendez-Vous{% endblock %}

{% block body %}
    <h1>Liste du Nombre Véhicule Maximum par Rendez-Vous</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre de Véhicule</th>
                <th>Date d'Application</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nombre_vehicule_maximum_rendez_vou in nombre_vehicule_maximum_rendez_vouses %}
            <tr>
                <td>{{ nombre_vehicule_maximum_rendez_vou.id }}</td>
                <td>{{ nombre_vehicule_maximum_rendez_vou.nombreVehicule }}</td>
                <td>{{ nombre_vehicule_maximum_rendez_vou.dateApplication ? nombre_vehicule_maximum_rendez_vou.dateApplication|date('d/m/Y') : '' }}</td>
                <td>
                    <a href="{{ path('app_nombre_vehicule_maximum_rendez_vous_show', {'id': nombre_vehicule_maximum_rendez_vou.id}) }}" class="btn-action show"><i class="fas fa-eye"></i></a>
                    <a href="{{ path('app_nombre_vehicule_maximum_rendez_vous_edit', {'id': nombre_vehicule_maximum_rendez_vou.id}) }}" class="btn-action edit"><i class="fas fa-edit"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="mt-4 d-flex justify-content-center">
        <nav>
            <ul class="pagination pagination-lg">
                {{ knp_pagination_render(nombre_vehicule_maximum_rendez_vouses) }}
            </ul>
        </nav>
    </div>

    <a href="{{ path('app_nombre_vehicule_maximum_rendez_vous_new') }}" class="btn-action new"><i class="fas fa-plus"></i> Nouveau</a>
{% endblock %}
