<form method="post" action="{{ path('app_jour_speciale_delete', {'id': jour_speciale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jour_speciale.id) }}">
    <button class="btn">Delete</button>
</form>
