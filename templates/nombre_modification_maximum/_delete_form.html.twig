<form method="post" action="{{ path('app_nombre_modification_maximum_delete', {'id': nombre_modification_maximum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nombre_modification_maximum.id) }}">
    <button class="btn">Delete</button>
</form>
