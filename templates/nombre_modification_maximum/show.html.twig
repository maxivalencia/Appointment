{% extends 'base.html.twig' %}

{% block title %}Nombre de Modification Maximum{% endblock %}

{% block body %}
    <h1>Nombre de Modification Maximum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nombre_modification_maximum.id }}</td>
            </tr>
            <tr>
                <th>Nombre de Modification</th>
                <td>{{ nombre_modification_maximum.nombreModification }}</td>
            </tr>
            <tr>
                <th>Date d'Application</th>
                <td>{{ nombre_modification_maximum.dateApplication ? nombre_modification_maximum.dateApplication|date('d/m/Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nombre_modification_maximum_index') }}" class="btn-action back"><i class="fa fa-arrow-left"></i> Retour</a>

    <a href="{{ path('app_nombre_modification_maximum_edit', {'id': nombre_modification_maximum.id}) }}" class="btn-action edit"><i class="fas fa-edit"></i> edition</a>

    {{ include('nombre_modification_maximum/_delete_form.html.twig') }}
{% endblock %}
