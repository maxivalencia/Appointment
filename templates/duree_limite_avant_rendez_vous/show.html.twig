{% extends 'base.html.twig' %}

{% block title %}Durée Limite Avant Rendez Vous{% endblock %}

{% block body %}
    <h1>Durée Limite Avant Rendez Vous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ duree_limite_avant_rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>Nombre d'Heure</th>
                <td>{{ duree_limite_avant_rendez_vou.nombreHeure ? duree_limite_avant_rendez_vou.nombreHeure|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date d'Application</th>
                <td>{{ duree_limite_avant_rendez_vou.dateApplication ? duree_limite_avant_rendez_vou.dateApplication|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_duree_limite_avant_rendez_vous_index') }}">Retour</a>

    <a href="{{ path('app_duree_limite_avant_rendez_vous_edit', {'id': duree_limite_avant_rendez_vou.id}) }}">edition</a>

    {{ include('duree_limite_avant_rendez_vous/_delete_form.html.twig') }}
{% endblock %}
